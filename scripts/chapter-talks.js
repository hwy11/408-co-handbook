window.CHAPTER_TALKS = {
  "2": {
    mode: "flow",
    eyebrow: "Chapter Walkthrough",
    title: "第二章串讲：你先别背补码和浮点，先想清楚机器里根本没有“数”",
    lead: "第二章真正要解决的不是进制转换，也不是背原码反码补码，而是一个更底层的问题：计算机里面只有 0 和 1，所谓整数、小数、正数、负数、字符、地址，全是人给这串位模式安排的解释规则。第二章就是从“位模式怎么解释”一路讲到“位模式怎么运算”。",
    flow: [
      "你先把人脑里的数字感拿掉。我们平时看到 -3、0.1、255，会天然觉得它们就是数，但机器没有这种直觉。机器里只有一格一格的二进制位，每一位只能稳定地表示 0 或 1。为什么非得是二进制？不是因为二进制看起来高级，而是因为电路最容易稳定地区分两种状态：高电平和低电平、导通和截止、充电和没充电。你让一个存储单元稳定地区分十种状态，工程成本和可靠性都会爆炸。所以二进制不是数学家的浪漫选择，而是硬件世界最现实的选择。",
      "既然机器只认 0 和 1，人就需要一种办法把长长的二进制写得短一点，于是八进制、十六进制就出现了。它们不是新的机器语言，只是二进制的缩写。三位二进制正好对应一位八进制，四位二进制正好对应一位十六进制。你看到 0xFF，不要觉得它和 11111111 是两种东西，它们只是同一串位模式的两种写法。**进制解决的是怎么书写，编码解决的是怎么解释**，这句话是第二章的第一根主线。",
      "接着问题就来了：同样一串 0 和 1，到底代表多少？如果你把 11111111 当无符号整数，它是 255；如果你把它当 8 位补码有符号整数，它是 -1。位没有变，解释变了，真值就变了。这就是编码的意义。原码的想法最像人：最高位当符号位，0 表示正，1 表示负，后面存绝对值。它直观，但硬件不喜欢。因为原码有 +0 和 -0 两个零，做加减法时还要判断符号、比较绝对值、决定走加法还是减法，电路会复杂。",
      "补码的出现就是为了让硬件少动脑子。你可以把 n 位二进制想成一个有 2^n 个刻度的圆环，往前走和往后走在这个圆环上可以互相替代。比如 8 位机器里，减 1 可以看成加 255，因为超过 8 位的进位会被丢掉，结果一样。这就是补码最核心的工程价值：**把减法变成加法，让符号位也能一起参加运算**。所以补码不是课本发明出来折磨人的，它是为了让 ALU 只靠加法器就能处理加减。",
      "补码还有一个很容易被忽略的结果：它只有一个 0，所以能多表示一个最小负数。8 位补码范围是 -128 到 127，而不是 -127 到 127。为什么正数少一个？因为 0 占了非负这一边的一个编码。你不要死背范围，直接想编码数量：8 位一共 256 种状态，分给 0 和正数 128 种，分给负数 128 种，所以负数能到 -128，正数只能到 127。",
      "C 语言里的整数类型转换，就是把这件事拉到程序层面。很多转换不是改变位，而是改变解释方式。一个 int 截成 char，可能只是低位留下，高位丢掉；一个 signed 变 unsigned，位模式可能不变，但解释方式从补码真值变成非负整数。符号扩展也不是随便补 1，而是为了在更宽位数下保持同一个补码真值。**做这种题先问三件事：位宽是多少、按有符号还是无符号解释、有没有截断或扩展。**",
      "运算方法这部分也不是孤立的新章节。既然数最后都落成位模式，运算就要靠电路对位进行处理。加法器从半加器、全加器开始，核心矛盾是进位怎么传。串行进位简单，但低位进位要一位一位传到高位，慢；并行进位想提前算出各位进位，硬件复杂，但速度快。移位看起来只是把位挪动，其实也要看解释规则：逻辑移位不管符号，空位补 0；算术右移要保留符号位，因为它是在维护补码有符号数的意义；循环移位则把移出去的位绕回来，常用于底层位操作。",
      "乘法和除法可以先想成加减和移位的组合。乘以 2 就是左移一位，除以 2 往往对应右移一位，但有符号数、舍入方向、溢出都会让事情变复杂。原码乘除适合把符号和数值分开处理，补码乘法则要让符号位和数值位在统一规则下工作。408 不要求你把所有硬件细节都背成电路图，但你必须知道：机器不是在“理解数学”，它是在按照编码规则移动、相加、取反、进位和截断。",
      "浮点数是第二章最容易被学散的地方。你把它想成二进制科学计数法就顺了。一个浮点数大致由符号位、阶码、尾数组成。符号位管正负，阶码管小数点移动多远，尾数管有效数字是什么。为什么要这么设计？因为定点数的小数点位置固定，范围很受限；浮点数把小数点位置也编码进去，就能用有限位数表示非常大或非常小的数。代价是精度不连续，很多十进制小数在二进制里根本表示不完。",
      "所以 0.1 + 0.2 不精确，不是语言 bug，而是 0.1 转成二进制就是无限循环，尾数位数有限，只能舍入。阶码位数越多，表示范围越大；尾数位数越多，有效精度越高。规格化是为了让同一个数尽量有唯一表示，让尾数最高有效位固定下来，别浪费位。IEEE 754 里阶码常用移码，是因为移码直接按无符号大小比较时，顺序更接近真实阶码大小，硬件比较方便。",
      "最后的大小端和对齐存储，其实是在问位模式落进内存以后怎么摆。一个 32 位整数要拆成 4 个字节放在连续地址里，大端把高位字节放低地址，小端把低位字节放低地址。数值本身没变，只是摆放顺序变了。对齐存储则是为了让硬件读写更顺，比如 4 字节整数最好从 4 的倍数地址开始，这样一次访存更容易取完整，不用跨边界拼接。第二章串起来就是一句话：**先把现实里的数变成位模式，再约定位模式怎么解释，再让电路按这个解释规则去运算，最后把结果按字节顺序放回内存。**"
    ]
  },
  "3": {
    mode: "flow",
    eyebrow: "Chapter Walkthrough",
    title: "第三章串讲：存储系统不是一堆器件名，而是在给 CPU 找一个现实可行的供货体系",
    lead: "第三章的第一性原理很简单：CPU 太快，数据太多，钱不够。你想要又快又大又便宜的存储器，但物理世界不允许，所以计算机只能把不同层次的存储器组织起来，让它们互相伪装成一个够快、够大、够便宜的整体。",
    flow: [
      "你先把 CPU 想成一个手速离谱的工人。它每秒能干很多活，但每干一步都要拿数据、放结果。如果所有东西都放在很远的仓库里，工人每次都跑仓库，速度就废了；如果所有东西都放在手边，手边空间又太小、太贵。于是计算机搞出一套分层存储：最常用的东西放手边，不太常用的放桌面，再不常用的放仓库。寄存器、Cache、主存、外存，就是这个层次从近到远的版本。",
      "这个体系为什么能骗过 CPU？靠局部性。程序不是完全随机访问数据的。刚访问过的变量，很可能马上又访问，这叫时间局部性；访问了数组 a[i]，接下来很可能访问 a[i+1]，这叫空间局部性。Cache 每次不只拿一个字节，而是拿一整个块，就是在赌空间局部性。**存储层次结构不是魔法，它是在利用程序访问行为的不随机。**",
      "主存这部分要先把“内存条”拆回芯片。SRAM 用触发器保存一位，速度快、不需要刷新，但每一位用的晶体管多，所以贵、面积大、容量不容易做大，适合做 Cache。DRAM 用电容存一位，电容会漏电，所以必须周期性刷新，读出后还要再生；它慢一些、控制复杂一些，但密度高、便宜、容量大，所以适合做主存。你不要背成 SRAM 高级、DRAM 低级，它们是在速度、成本、容量之间做不同取舍。",
      "ROM 家族也别被名字骗了。现代语境里 ROM 的关键不是“永远不能写”，而是掉电后数据还在。PROM、EPROM、EEPROM、Flash 的差别主要在能不能改、怎么擦、擦写粒度多大。Flash 能按块擦除，所以适合做 U 盘、SSD、固件存储。这里的主线仍然是：有些存储器追求运行时快速读写，有些追求断电保存，有些追求便宜大容量。",
      "主存容量扩展题其实就是拼积木。CPU 想要一个容量和字长都满足要求的主存，但单个芯片往往不够。字长不够，就让多片芯片并联，每片负责数据总线的一部分，这叫位扩展；字数不够，就做多组芯片，用片选信号决定哪一组工作，这叫字扩展。比如目标 64K×16，芯片 16K×8，那每组要两片拼成 16 位，组数要四组扩到 64K，总共八片。你把这个想成横向拼宽、纵向拼多，就不会乱。",
      "CPU 和主存连接，本质不是画线，而是分门牌。CPU 发出一串地址，低位地址通常进入芯片内部，选择这个芯片里的哪个单元；高位地址进入译码器，决定哪一组芯片被选中。全译码把相关高位地址都用上，所以一个物理单元只对应一个地址范围；部分译码省硬件，但有些高位没参与判断，于是多个地址可能访问到同一物理单元，形成地址重叠。**地址题永远先确认编址单位，再确认低位给片内地址，高位给片选。**",
      "外存这节你不能上来就背寻道时间。先想物理画面：机械硬盘是一张张高速旋转的盘片，数据写在一圈一圈磁道上，磁头要先移动到目标磁道，再等目标扇区转到磁头下面，然后才能真正传数据。所以一次随机访问慢，不是传输本身一定慢，而是寻道和旋转等待很贵。顺序读写快，是因为定位成本被摊薄了。SSD 没有磁头和盘片，所以随机读强很多，但 Flash 写入前常要擦除，控制器还要做磨损均衡、垃圾回收和映射管理，所以 SSD 也不是“没有代价的快”。",
      "Cache 是第三章最核心的抓分区。它站在 CPU 和主存之间，目的不是扩大容量，而是弥补速度差。CPU 访问一个地址时，先看 Cache 里有没有对应块。如果命中，直接用快路径；如果未命中，再去主存把块调进来。关键是地址怎么拆：块内地址决定块里哪个字节，索引决定查 Cache 哪一行或哪一组，标记用来判断这一行里放的是不是你要的主存块。直接映射就是一个主存块只能去固定 Cache 行，速度简单但冲突多；全相联是哪里都能放，冲突少但比较电路复杂；组相联折中，先定位组，再在组内相联查找。",
      "替换算法和写策略也要顺着 Cache 的现实问题理解。组满了，新块进来必须踢掉旧块，于是有 FIFO、LRU、随机等替换策略。CPU 写 Cache 时，如果立刻写主存，叫写直达，简单一致但慢；如果先只改 Cache，等块被替换时再写回主存，叫写回，快但要维护脏位和一致性。多核或多级 Cache 里，同一个内存地址可能有多个副本，谁改了、别人知不知道，这就是 Cache 一致性问题的来源。",
      "虚拟存储器解决的不是速度，而是程序看到的地址空间问题。程序以为自己有一大片连续内存，实际上物理主存可能碎片化、可能不够用，操作系统和硬件在背后做地址翻译。CPU 产生虚拟地址，页表把虚页号翻译成物理页框号，页内偏移不变。TLB 是页表项的小缓存，省得每次翻译都去内存查页表。缺页就严重多了，说明要访问的页面根本不在主存，需要操作系统从外存调入，代价远高于普通 Cache miss。",
      "段式、页式、段页式也不是孤立概念。页式按固定大小切，便于主存管理，但程序逻辑感弱；段式按程序逻辑切，比如代码段、数据段、栈段，便于保护和共享，但段长可变，容易产生外部碎片；段页式先按逻辑分段，再在段内分页，兼顾逻辑结构和固定页框管理，但地址转换更复杂。第三章最后要收成一句话：**Cache 让主存看起来更快，虚拟存储器让主存看起来更大，外存让数据长期留下，主存连接和芯片组织则告诉你这些抽象最后怎么落到硬件。**"
    ]
  },
  "4": {
    mode: "flow",
    eyebrow: "Chapter Walkthrough",
    title: "第四章串讲：你先别背 ISA，先把 CPU 为什么需要“指令”这件事想明白",
    lead: "第四章要从一个很朴素的问题开始：CPU 只是一堆电路，它不会天然理解 C++，也不会天然理解 if、for、函数调用。想让这堆电路按人的意图干活，就必须把人的意图翻译成一条一条机器能识别的命令。第四章讲的就是这套命令怎么设计、怎么找数、怎么把高级语言落到机器动作上。",
    flow: [
      "你先把画面放简单一点：内存里放着一串 0 和 1，CPU 每次从里面取出一小段，把它当成一条命令，然后按照这条命令去做事。问题来了，同样都是 0 和 1，CPU 怎么知道前几位是在说“做加法”，后几位是在说“去哪个地方取数”？这就需要一套约定。**指令系统就是这套约定**。更正式一点叫 ISA，也就是指令集体系结构，但你先别被英文吓住，它本质上就是 CPU 对外承诺的“我能听懂哪些机器命令、每条命令长什么样、命令里的数该怎么解释”。",
      "所以 ISA 不是上来就讲“CPU 内部怎么实现”。你现在先把它当成一份机器说明书：这台机器有多少个程序能用的寄存器，支持加减乘除还是只支持简单加法，指令里怎么写操作数，访存用什么规则，遇到异常和中断时表现成什么样，这些都是软件和硬件必须提前说好的东西。编译器按照这份说明书生成机器码，CPU 按照这份说明书解释机器码。只要这份说明书不变，CPU 内部是五级流水、十级流水，还是把复杂指令拆成微操作执行，软件通常不用管。**这就是为什么说 ISA 是软件和硬件之间的合同**，但这个合同不是玄学，它就是为了让“二进制命令”有稳定含义。",
      "接着你就能理解什么叫指令格式了。一条机器指令不是一句中文，它是一段固定长度或不固定长度的二进制。既然是一段二进制，就必须规定每一部分分别干什么。最常见的切法是：一部分叫操作码，用来告诉 CPU 这条指令要做什么，比如加法、传送、跳转、读内存；另一部分叫地址码，用来告诉 CPU 操作数在哪里，或者结果要写到哪里。你可以把操作码想成外卖订单里的“我要买什么”，地址码想成“去哪取、送到哪”。如果没有这个格式，CPU 拿到 0010011010 只会懵，因为它不知道哪几位是动作，哪几位是位置。",
      "指令格式为什么在 408 里会变成计算题？因为指令长度有限。假设一条指令就 16 位，操作码多占几位，能表示的操作种类就多，但留给地址的位数就少，能直接写进去的地址范围就小；地址字段多一些，能同时说明多个操作数，但整条指令就更长，取指和存储都更贵。所以三地址、二地址、一地址、零地址不是为了背概念，它们是在不同机器设计里做取舍。三地址指令很直观，像 A = B + C，一条指令里能说清两个源操作数和一个结果位置，但它长；二地址指令常常让其中一个位置既当源操作数又当结果，比如 A = A + B，省了字段，但 A 原来的值会被覆盖；一地址指令通常默认有个累加器 ACC 参与运算，指令只写另一个操作数；零地址指令常见于栈机器，操作数默认从栈顶拿，结果再压回栈顶。",
      "扩展操作码就是这块最容易被死背的地方。你别把它想成公式，想成“编码空间不够用了，设计者开始抠门”。有些指令需要三个地址字段，有些指令只需要一个地址字段，甚至不需要地址字段。如果所有指令都强行用同样长的操作码，就会浪费很多位。扩展操作码的思路是：先给地址数多的指令留较短的操作码，因为它们还要装很多地址；对于地址数少的指令，把省下来的地址字段拿来继续扩展操作码，于是同样一条指令长度里，可以塞下更多种操作。考试让你算最多能设计多少条指令，本质就是在问：哪些编码已经被前一类指令占了，哪些编码还能继续往后扩。",
      "可是只知道指令里有地址码还不够，因为地址码不一定直接就是操作数的位置。这里就引出寻址方式。寻址方式的意思很土：**指令里的那个字段，到底怎么才能变成 CPU 真正要用的数**。如果字段本身就是数，比如把 5 加到某个寄存器里，这叫立即寻址，它最快，因为不用再去内存找这个操作数；如果字段写的是内存地址，CPU 按这个地址去内存取数，这叫直接寻址；如果字段写的不是最终地址，而是一个存着最终地址的位置，CPU 要先去这个位置读出真正地址，再按真正地址取操作数，这叫间接寻址。间接寻址更灵活，但慢，因为多访存一次。",
      "寄存器相关的寻址也可以用同一条线理解。寄存器寻址就是操作数已经在寄存器里，CPU 直接拿，速度快，指令字段也短，因为寄存器编号比内存地址短得多。寄存器间接寻址则是寄存器里放的不是操作数，而是内存地址，CPU 拿寄存器内容当地址去内存找数。变址寻址和基址寻址看起来都像“一个基准加一个偏移”，但用途不一样。变址更像数组访问：数组起点加上下标偏移，就能找到 a[i]；基址更像程序搬家：程序整体装到内存另一个位置后，只要基址变了，指令里的相对偏移仍然能用。相对寻址则常常用于跳转，目标地址等于 PC 加偏移，所以循环和分支很适合它，因为代码整体移动时，相对距离不变。",
      "到这里你会发现，第四章前半部分其实是在解决“机器指令怎么表达”。有了表达能力，后半部分就开始问：高级语言怎么被翻译成这些机器指令？CPU 不认识 if。if 在机器眼里就是先比较，再根据条件决定要不要跳到另一个地址。CPU 也不认识 for 和 while。循环在机器眼里就是先判断条件，执行循环体，更新变量，然后跳回去再判断。CPU 更不认识函数调用。函数调用在机器眼里是一套保存和恢复现场的纪律：参数要放到约定位置，返回地址要保存，局部变量要有地方放，某些寄存器如果调用前后还要保持原值，就得按约定保存，函数执行完再恢复，然后跳回返回地址继续跑。",
      "栈帧这件事尤其值得你真正想明白。函数不是凭空“进入”和“返回”的。每调用一次函数，机器就要给这次调用准备一块活动空间，里面可能有返回地址、旧帧指针、局部变量、临时数据、溢出的参数。递归为什么能成立？不是因为语言层面说它可以，而是因为每一层调用都有自己的栈帧。f(3) 调 f(2)，f(2) 再调 f(1)，它们不是共享一份局部变量，而是栈上一层压一层，各有各的现场。等最里面返回，再一层层弹回去。你如果能在脑子里看到栈一层层压入和弹出，过程调用这部分就不是背诵题了。",
      "最后再看 CISC 和 RISC。这个东西也别学成“复杂指令系统计算机”和“精简指令系统计算机”的名词对照。它本质是在问：复杂性放在哪里。CISC 倾向于让单条指令干更多事，指令种类多、格式多、寻址方式多，一条指令可能又访存又运算，代码看起来紧凑，但 CPU 译码和流水线实现会更麻烦。RISC 倾向于把指令做得简单、规整、长度固定，尤其强调 Load/Store：只有专门的 load/store 指令访问内存，算术逻辑运算主要在寄存器之间做。这样编译器可能要生成更多条指令，但硬件控制简单，流水线更好排。",
      "所以第四章真正的主线不是“ISA、指令格式、寻址方式、机器级代码、CISC/RISC”五个标题并排摆着，而是从一件事一路推出来：人写的程序必须变成 CPU 能读的二进制命令；二进制命令必须有格式，否则 CPU 不知道哪几位表示动作、哪几位表示位置；位置字段还要有解释规则，否则 CPU 不知道这个字段本身是数、是地址、还是地址的地址；高级语言里的选择、循环、函数调用，最后都要落到比较、跳转、寄存器、内存和栈；而不同处理器设计，又会在“单条指令更强”还是“指令更简单更规整”之间做取舍。**第四章学透以后，你看到一段机器码或汇编，脑子里不应该先冒术语，而应该能看见 CPU 正在取哪条指令、按什么格式切开、去哪里找数、下一步跳到哪里。**"
    ]
  },
  "5": {
    mode: "flow",
    eyebrow: "Chapter Walkthrough",
    title: "第五章串讲：CPU 不是玄学大脑，而是一台把指令一步步落成动作的机器",
    lead: "第五章要接住第四章。第四章说机器指令长什么样、怎么解释，第五章就问：CPU 拿到这条指令以后，到底怎么让寄存器、ALU、主存、控制信号配合起来，把这条指令真的执行完。",
    flow: [
      "你先别把 CPU 想成一个抽象的“大脑”。这个比喻太粗，会让你忽略考试真正问的东西。CPU 更像一个按节拍工作的工厂：控制器负责发命令，运算器负责加工数据，寄存器负责临时存放东西，总线和数据通路负责把东西送来送去。程序不是一次性被 CPU 理解的，而是一条指令一条指令被取出、分析、执行。",
      "CPU 的功能可以顺着一条指令自然推出。它要能控制指令顺序，所以有指令控制；它要能把一条指令拆成具体动作，所以有操作控制；它要能安排每个动作在哪个时钟节拍发生，所以有时间控制；它要能对数据做加减与逻辑处理，所以有数据加工；它还要能面对外部请求或内部错误临时打断当前流程，所以有中断处理。这些不是五个孤立名词，而是“让程序能持续正确运行”这件事拆出来的五个职责。",
      "寄存器这部分要和数据流绑在一起理解。PC 保存下一条要取的指令地址，IR 保存当前正在执行的指令，MAR 保存要访问的主存地址，MDR 保存从主存读来或准备写回主存的数据，通用寄存器保存程序临时数据，PSW 保存条件码和状态信息。你不要把寄存器当清单背，它们都是因为 CPU 内部每一步都需要“临时放一下”而存在的。",
      "一条指令怎么执行？先取指。PC 把地址送出去，地址进入 MAR，主存按这个地址把指令送到 MDR，再装入 IR，PC 更新到下一条。然后译码，控制器看 IR 里的操作码，判断这条指令是加法、访存、跳转还是别的。再往下可能要取操作数，可能要让 ALU 运算，可能要访问主存，可能要把结果写回寄存器。**指令周期不是背阶段名，而是看数据从哪里来、经过哪里、最后到哪里去。**",
      "数据通路就是这些数据能走的路。ALU、寄存器、内部总线、选择器、MAR、MDR 连在一起，构成 CPU 内部的数据流网络。但路在那里还不够，谁什么时候把数据送上总线，谁什么时候接收，ALU 做加法还是减法，都需要控制信号。控制器的价值就在这里：它不亲自算，它指挥每个部件在正确时刻做正确动作。",
      "控制器有两种典型做法。硬布线控制器像把控制逻辑直接焊死在电路里，输入当前指令、节拍、状态，就由组合逻辑生成控制信号。它快，但改起来难。微程序控制器则像给控制器内部也写了一套“小程序”，每条机器指令对应一段微指令序列，微指令里编码了要发哪些控制信号。它灵活，适合复杂指令，但取微指令本身也要时间。这里最重要的分层是：**机器指令是程序员和编译器看到的，微指令是控制器内部用来实现机器指令的。**",
      "异常和中断是 CPU 执行流程里最像现实世界的一部分。程序本来顺着 PC 往下跑，突然除零、溢出、非法指令、缺页，或者外设告诉 CPU“我这边好了”，CPU 就不能继续装作没事。它要保存现场，找到处理程序入口，转去处理，处理完再尽量恢复回来。异常更多和当前指令有关，中断更多来自外部事件。现场为什么要保存？因为不保存 PC、PSW、必要寄存器，处理完就不知道从哪继续，也不知道原来状态是什么。",
      "流水线是第五章真正容易考计算和理解的地方。你可以把一条指令执行分成取指、译码、执行、访存、写回几个阶段。非流水时，一条指令走完五步，下一条才开始；流水线则让第一条在译码时，第二条就开始取指，第一条执行时，第二条译码，第三条取指。它的本质不是让单条指令更快，而是让多条指令重叠，提高单位时间完成指令的数量。**流水线主要提高吞吐率，不是神奇缩短每条指令所有工作。**",
      "流水线的问题也顺着这个画面来。结构冒险就是两个阶段同时抢同一个硬件资源，比如同时想用同一个存储器端口；数据冒险就是后一条指令太早需要前一条指令的结果，而结果还没写回；控制冒险就是遇到分支跳转，CPU 不知道下一条到底该取哪条。解决办法也很直观：资源冲突就加资源或暂停，数据没到就转发或停顿，分支不确定就预测、延迟或清空错误路径。",
      "高级流水线、多发射、乱序这些技术，都是在想办法让执行单元别闲着。超标量希望一个周期发射多条指令，乱序执行希望不被一条慢指令卡住，只要不破坏程序最终语义，能先执行的就先执行。硬件多线程也是这个逻辑：一个线程等内存时，别让核心发呆，切到另一个线程做事。多核则更进一步，直接放多个核心并行工作。",
      "多处理器最后会把第三章的 Cache 一致性带回来。多个核心各有自己的 Cache，如果核心 A 改了某个变量，核心 B 的 Cache 里还有旧值，就会出问题。共享内存多处理器要靠一致性协议维护同一地址的多个副本。第五章串起来就是：**第四章给出指令含义，第五章把含义拆成微操作，再用数据通路承载数据，用控制器安排时序，用流水线和并行技术提高吞吐，用异常中断处理正常顺序之外的事件。**"
    ]
  },
  "6": {
    mode: "flow",
    eyebrow: "Chapter Walkthrough",
    title: "第六章串讲：总线不是一根线，而是计算机内部的交通规则",
    lead: "第六章短，但不能当水章。CPU、主存、I/O 都要交换信息，如果每两个部件都单独拉线，机器会复杂到不可维护。总线的出现，就是让大家共用道路；而只要共用，就一定会有抢路、排队、定时和带宽问题。",
    flow: [
      "你先从连接复杂度想。假设 CPU、主存、磁盘控制器、网卡、显卡、键盘接口都要彼此通信，如果每两个设备之间都单独连一套线，设备一多，线路数量会爆炸。总线的做法是：大家挂到一组公共线路上，需要通信时按规则使用。好处是硬件简单、扩展方便；坏处是同一时刻不能所有设备都同时说话，否则信号会互相冲突。**总线的本质不是线，而是共享线路加使用规则。**",
      "系统总线通常分成地址总线、数据总线、控制总线。地址总线回答“这次要访问哪里”，比如主存哪个单元或 I/O 哪个端口；数据总线回答“这次传什么”，读操作时数据从从设备到主设备，写操作时数据从主设备到从设备；控制总线回答“这次怎么传、什么时候传、谁请求、谁响应”，比如读写信号、中断请求、总线请求、总线允许、时钟、复位。地址线多主要扩大可寻址范围，数据线宽主要增加一次能传的数据量，这两个别混。",
      "总线结构是在简单和并行之间取舍。单总线结构最容易理解，CPU、主存、I/O 都挂一条公共总线，便宜、清楚、扩展方便，但所有传输都抢一条路，瓶颈明显。双总线、三总线把通路拆开，比如让 CPU 和主存之间有更直接的通道，I/O 再通过另一条通路接入，这样某些通信可以并行，但控制和硬件成本会上升。考试问总线结构优缺点时，不要只背名字，要问：**哪些传输会互相堵，哪些传输能同时跑。**",
      "总线标准也不是为了增加名词量。不同厂家做的设备要能插在一起，就必须约定机械形状、电气电平、信号含义、传输流程。机械特性规定接口长什么样，电气特性规定电压、电流、时序范围，功能特性规定每根线表示什么，过程特性规定一次通信按什么步骤来。没有标准，设备就像说不同语言的人，插上也不一定能交流。",
      "一次总线传输可以想成一次上路。主设备先请求总线，仲裁逻辑决定谁获得使用权，主设备把地址和命令放到总线上，从设备识别到“这是找我的”，然后准备数据或接收数据，传输完成后释放总线。这里的主设备不一定永远是 CPU。DMA 控制器在搬运 I/O 数据时，也会临时成为主设备，直接和主存交换数据，所以它也要参与总线仲裁。",
      "仲裁的意义就是解决抢路。多个主设备同时想用总线，如果没人裁决，就会一起驱动总线，数据直接乱掉。集中式仲裁把决定权交给一个仲裁器，常见思路有链式查询、计数器查询、独立请求。链式查询像排队传话，硬件简单，但离仲裁器近的设备优先级天然高，远的可能饿死；独立请求给每个设备单独请求线，速度快、优先级灵活，但线多、硬件复杂。分布式仲裁则让设备按某种规则自己竞争，复杂度换灵活性。",
      "定时解决的是另一个问题：拿到路以后，双方怎么知道什么时候地址有效、什么时候数据有效、什么时候可以撤信号。同步总线用统一时钟，所有动作卡在固定节拍上，控制简单，速度也容易做高，但要求设备速度差异不能太大，因为慢设备跟不上就会拖后腿。异步总线不用统一时钟，而靠请求和应答握手，你准备好了告诉我，我收到后再告诉你，适合速度差异大的设备，但控制更复杂。",
      "半同步可以看成折中。整体还是按统一时钟推进，但如果从设备太慢，可以通过 WAIT、READY 之类信号插入等待周期。这样既保留同步总线的基本节奏，又允许慢设备喘一口气。你看到题里出现 READY、WAIT、ACK 这些词，就别只盯公式，要想到这背后是在协调双方速度差。",
      "带宽题看起来简单，但最容易丢单位。总线带宽就是单位时间能传多少数据，基本思路是每次传输的数据量乘每秒传输次数。数据总线 64 位，频率 100MHz，每周期传一次，就是 64×100M bit/s，也就是 800MB/s。可如果一次传输要多个时钟周期，或者一个周期能传多次，或者题目给的是突发传输，你就不能机械相乘。bit 和 Byte 差 8 倍，MHz 是每秒百万周期，这些细节会直接决定答案对不对。",
      "第六章最后要和后面的 I/O 连起来。中断需要通过控制线通知 CPU，DMA 需要争用总线把数据搬到主存，I/O 接口要挂在总线上响应地址和命令。总线不是孤立小章，它是 CPU、主存、外设之间信息流动的公共地基。把它串起来就是：**为了少拉线，大家共享通路；共享就要仲裁；设备速度不同就要定时；通路能力有限就要算带宽。**"
    ]
  }
};

window.renderChapterTalk = function renderChapterTalk(chapter) {
  const talk = window.CHAPTER_TALKS && window.CHAPTER_TALKS[chapter];
  if (!talk) return `<div class="card">本章串讲内容加载中...</div>`;
  if (talk.mode === "flow") {
    return `
      <section class="talk-hero talk-hero-flow">
        <div class="talk-eyebrow">${talk.eyebrow}</div>
        <h1>${talk.title}</h1>
        <p>${talk.lead}</p>
      </section>
      <article class="talk-flow">
        ${talk.flow.map(paragraph => `<p>${paragraph.replace(/\*\*(.*?)\*\*/g, "<strong>$1</strong>")}</p>`).join("")}
      </article>
    `;
  }
  return `<div class="card">本章串讲内容加载中...</div>`;
};
